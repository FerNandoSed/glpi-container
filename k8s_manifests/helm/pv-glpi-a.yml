apiVersion: v1
kind: PersistentVolume
metadata:
  name: glpi-data-a
spec:
  capacity:
    storage: 10Gi
  accessModes:
  - ReadWriteMany
  nfs:
    path: "/data/glpi-a"
    server: nfs-server.default.svc.cluster.local